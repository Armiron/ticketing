export enum Subjects {
  TicketCreated = 'ticket:created',
  OrderUpdated = 'order:updated',
}

//How to use
// const printSubject = (subject: Subjects) => {};
// //Helps you out with strings and such
// printSubject(Subjects.TicketCreated);
